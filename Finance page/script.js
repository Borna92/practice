// const calculate = document.querySelector('.operation button');

// calculate.addEventListener('click', () => {
//     const first = document.querySelector('.search-box .first').value;
//     const second = document.querySelector('.search-box .second').value;
//     const operation = document.querySelector('.search-box button').value;
//     const result = document.querySelector('.result');

//     if (first === 0 || second === 0)
//         return;

//     const res = eval(`${first} ${operation} ${second}`);

//     result.innerHTML = res;
    
// })